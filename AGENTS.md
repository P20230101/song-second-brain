# “松的第二大脑”维护规则

## 目标与分工

这是 Obsidian 与 GitHub Pages 共用的长期研究库。用户提供原始资料、真实设备记录和研究判断；Codex 负责整理可复用结论、维护链接、索引和日志，不替代用户确认实验事实。

## 入口顺序

处理任何库内任务时依次阅读：

1. `AGENTS.md`
2. `index.md`
3. 对应项目的 `README.md`
4. 相关页面和当天日报

不要无目的扫描整个 Vault。

## 目录约定

- `00_Inbox/`：待整理内容；`00_Inbox/Raw/` 保存原始来源和下载清单。
- `10_Projects/PA12/`：PA12 全场识别、VFM、文献和投稿工作。
- `10_Projects/双轴试验/`：双轴试样、夹具、Abaqus、实验数据和文献。
- `10_Projects/第二大脑/`：系统规则、研究上下文和自动化约定。
- `10_Projects/科研论文/`：其他论文项目，除非明确关联，不并入 PA12。
- `20_Knowledge/`：长期知识、AI、Obsidian 和公开 Wiki 页面。
- `30_Reports/研究报告/日报/`：按 Asia/Shanghai 日期写入每日闭环报告。
- `40_Reference/`：模板和参考资料。
- `90_Archive/`：已结束项目和历史文档。
- `attachments/`：图片和附件；不要把工具缓存放进 Vault。

## Raw sources / The wiki / The schema

- Raw sources 只读：不得改写、重命名或删除 `00_Inbox/Raw/`、PDF、CSV/DAT、实验原始数据、设备记录和下载清单。
- The wiki 位于 `20_Knowledge/Wiki/`，项目结论位于对应 `10_Projects/*/`；优先更新已有页面并建立交叉链接。
- 本文件和 `index.md` 是 Schema 入口。只有真实使用暴露规则缺口时才修改，不添加推测性流程。

## 编辑与链接规则

- Wiki、项目入口和日报使用简体中文；文件名清晰稳定。
- 保留 Obsidian 双链和 Markdown 相对链接；移动或重命名时同步修复链接。
- 来源性陈述必须链接到可追溯来源；推断、待验证问题和用户尚未确认的内容要明确标注。
- 不修改 `.obsidian` 配置，除非用户明确要求；不把密钥、隐私和原始数据提交到公开仓库。
- 不删除原始笔记。只可删除或归档当天明确重复、空白或无效的 Markdown 草稿，并在日报记录。

## 每日闭环

每日 21:00（Asia/Shanghai）只处理当天新增或修改的 Markdown、当天日报和当天相关日志，不把历史日期混入当天。提取并合并：

- 当前研究状态
- 已知证据、来源与 DOI
- 关键数据和可复现实验步骤
- 已作决定、待确认问题、下一步行动

写入 `30_Reports/研究报告/日报/YYYY-MM-DD.md`，必要时追加 `10_Projects/PA12/log.md`、`10_Projects/双轴试验/log.md` 或系统日志，并维护 `index.md` 与相关 README。新方向无法确定归属时放入 `00_Inbox/` 并列入待确认项，不擅自覆盖既有项目。

设备参数、相机参数、触发方式和事件记录以用户当天提供的真实记录为准，不用通用型号替代。

## 研究上下文与文献

- PA12 研究上下文放在 `10_Projects/PA12/`，双轴研究上下文放在 `10_Projects/双轴试验/`；先读上下文再分析。
- 文献 PDF 默认保存于 `00_Inbox/Raw/参考文献/<项目>/PDF原件/`，元数据和 `download_manifest.json` 与原件同目录；原始 PDF 只读。
- 公开检索可使用 arXiv、Semantic Scholar 等可追溯来源；没有确认的 PDF 地址不得凭空拼接。

## GitHub Pages 与公开提交

公开 Wiki 变更必须先执行 `git diff --check`，确认不含原始数据、隐私、密钥、私有工具和未确认实验文件，再提交并推送。其余内容只保存在本地。当天没有实质变化时保持安静。

## 修改边界

只修改当前请求所需的文件；不吞掉错误。链接失效、事实冲突、来源不可获取或归属不确定时直接记录并请求确认。
