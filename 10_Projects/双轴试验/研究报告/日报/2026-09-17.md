---
title: 2026-09-17 科研日报
date: 2026-09-17
project: PA12 双轴-DIC-VFM
---

# 2026-09-17 科研日报

## 当前研究状态

双轴试样—夹具仿真已从文献方案进入可运行的基准筛选阶段。46 篇跨材料文献已经结构化交叉，Abaqus 已完成试样几何、夹具传力和压缩稳定性三个层级的数值基准。真实 PA12 试样尺寸、夹具接触和材料曲线尚未校准，因此当前结论用于方案筛选，不作为实验定量结论。

## 主线修正：真实 PA12 中心试样

今天进一步核对了 `XY-0.1-02` 的实际数据，主线从通用夹具回到真实中心十字试样：

- 原始主序列为 259 张 JPG（`Img000000`–`Img000258`），对应 259 个逐帧 `.dat`；首帧可见中心散斑和四臂开缝，末帧可见中心断裂。全量 `.dat` 已逐个解压并解析成功，说明完整逐点场入口实际存在。
- `Job.m2inp` 实际含 1 个参考帧和 258 个变形帧，修正了旧表中的“仅 10 帧”错误。
- 工程中心 ROI 为 9 点多边形，像素包围盒约 449×441 px；工程记录的转换因子为 0.097519 mm/px，但导出单位仍设为 pixels，因此不能直接把当前 `U/V` 当作 mm。
- 用户确认当前中心减薄厚度为 1.0 mm；按用户模型/论文基线，名义几何采用每臂 7 条狭缝、狭缝长 40 mm、圆角 R=3 mm、狭缝端部间距 ts=0.2 mm。外轮廓和臂宽仍需从模型/尺寸图数值化。
- `vertical_all_v1/reports/manifest.csv` 已明确列出 S16（即 XY-0.1-02）的 259 个原始图像帧 0–258；`DIC-xy_0.2` 的 133 个逐点 CSV 文件名明确对应原始帧 0–132。帧 0–131 的相关质量稳定，帧 132 质量突变；原始 `.dat` 已补足 133–258 的逐点场入口，只是尚未全部导出为简洁 CSV。
- `.dat` 点数审计显示：0–250、252 帧为 10 505 点；251、253–257 帧为 10 504 点；258 帧为 7 599 点。前 258 帧均可解析，末帧的掉点必须通过有效掩膜处理，不能按完整场直接进入识别。
- `Press` 原始加载段为负，`Pos` 四通道均增加约 2.8 mm；按向外位移为拉伸、拉伸取正的工作约定，现有逐照片表保留正号力，但同步状态仍为端点估计。
- 259 帧端点映射的平均帧间隔为 0.107504 s（约 9.302 Hz）；四通道平均力最大值出现在帧 236，故峰值事件按 235–236 窗口记录。

本轮又对 `yuan\\data` 全集扫描：14 个 XY/XZ 试验目录共 6,327 个 MatchID `.dat`（XY 5,295、XZ 1,032）全部可解压解析。XY 的现有照片—力候选表 5,309 行已按试验编号/帧号与 DAT 审计关联；X-05 有 14 张照片没有对应 DAT。XZ 目前只有可读 DAT 和图像，没有逐照片力时序，因此只保留字段/质量审计，不把它们伪装成可用 VFM 输入。

已在 `D:\C盘迁移\Desktop\yuan\analysis\real_specimen_XY-0.1-02` 生成 259 行照片—力精简表、133 帧 DIC 质量摘要、133 帧文件名清单、133 行照片—力—DIC 候选合并表，以及 259 行照片—力—MatchID `.dat` 候选表和 `.dat` 帧审计表；合并表的时间仍是端点估计，不冒充共同触发。

详见 [真实 PA12 中心试样｜证据与建模契约](../../wiki/真实PA12中心试样-证据与建模契约-2026-09-17.md)。

## 今日形成的证据

- 文献矩阵共 46 篇：17 篇为 2023—2026 年，29 篇达到全文方法层级，16 篇形成可直接执行的方法证据。
- 几何层完成 30 个独立设计 × 3 条加载路径，加 1 个方形基准，共 91 个成功 ODB；所有 90 个设计—路径的 VFM 矩阵秩为 4。
- 最大合成参数恢复误差约 5.66%，最大内外虚功相对差约 0.0474%；该结果只验证数值闭环。
- 夹具传力层 3 个 ODB 成功：柔顺夹具在相同执行器位移下只传递刚性基准约 77.14% 的 X 向力；5% X 向执行器不平衡产生约 1.62% 的对向反力不对称。
- 压缩稳定性层 2 个 ODB 成功：自由压缩的 DIC 窗口内最大面外位移约 4.0166 mm；理想支撑环将其压到约 0.0000193 mm。支撑环是理论上界，不是实物夹具定型。
- 代理模型使用实际成功 ODB 和预留几何测试集，但三个响应的最大相对误差为 27.92%—40.14%，未达到 90% 精度要求，保持探索状态。

## 今日决定

1. 以 G13、G15、G10、G25、G07 作为下一轮局部几何 DOE 的候选族，不直接输出加工尺寸。
2. 真实夹具模型按“等效刚度校准—真实结构—接触/预紧—对中误差—压缩防屈曲”逐层建立。
3. 机器学习只用于提出下一批真实 Abaqus 采样点，禁止用预测样本冒充 ODB 结果。
4. 压缩路线必须保留中心 DIC 窗口并检查面外运动；拉伸候选不自动视为压缩合格。

## 今日生成的文件

- [46篇文献与Abaqus结果决策页](../../wiki/双轴试样夹具仿真优化-46篇文献与Abaqus结果-2026-09-17.md)
- [全试样 MatchID 审计与准入表](../../科研总项目-PA12-DIC-VFM-20260917/ALL_TRIALS_AUDIT.md)
- 本地文献矩阵、全文证据、交叉综合与检索日志；
- 5 个新增边界基准输入及成功 ODB；
- 夹具传力、压缩稳定性 CSV 与汇总图；
- 按设计分组、预留测试集的代理模型审计文件。

## 下一阶段

1. 从当前模型/尺寸图录入外轮廓、臂宽及 `d/ds/ws/Rs`，中心厚度按 1.0 mm 固定；
2. 用 259 行 `.dat` 审计表检查字段、单位和有效点掩膜，先选择质量稳定帧段；末帧 258 仅作破坏事件参考，不能直接当完整场；
3. 以 0、235–236、237、258 为首帧、峰值、峰后、末帧候选窗口，回看中心 ROI 与力信号；
4. 将真实 PA12 单轴曲线和目标加载比写入材料与边界，生成真实名义几何 Abaqus 模型；
5. 在共同触发证据缺失时，把端点映射作为时移敏感性变量，再进入代码 VFM；夹具仿真 DOE 暂停扩展。

## 待确认问题

- 当前实际十字形试样的 CAD 或带尺寸草图在哪里？
- 四个加载端的夹持方式是销、夹板还是复合结构？
- 是否已有空载或刚性连接件的四通道力—位移记录？
- 压缩防屈曲件允许保留多大的中心观察窗和间隙？

## 21:00 审查

- 将 `实验/PA12-双轴-DIC-VFM/results/试样夹具仿真优化结论.md` 移入 `results/审计/`；日常结果根目录恢复为两个固定中文入口，既有 Wiki 引用同步改为审计路径。
- 晚间新增的 `constitutive_virtual_experiment` 已生成 3 个合成硬化模型、3 条加载路径和 9 个 Abaqus 作业；`input_manifest.json` 明确其为通用合成基准，不能用于 PA12 结论。
- 数据集维度契约测试通过；本构积分和输入生成测试尚不能完成，运行环境缺少 `scipy`，且测试引用的 `identifier` 模块不存在。因此该虚拟试验维持“未验证本地工作”，不写入 Wiki、不纳入 Git 提交。

下一步：在具备 `scipy` 的环境中补齐 `identifier` 后重跑完整测试；仅在全套测试通过且独立留出路径验证完成后，才评估其作为 VFM 模型判别基准的价值。
