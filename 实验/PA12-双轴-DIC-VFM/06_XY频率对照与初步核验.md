---
title: XY 方向照片—力传感器频率对照与初步核验
date: 2026-09-12
status: preliminary
source: 用户提供的两张单方向拉伸频率截图
---

# XY 方向照片—力传感器频率对照与初步核验

## 结论先行

这两张表还不能整体判定为“已同步正确”，但已经能分出两类结果：

- **图 2（暂记为方向 B）**：三档速度的“速度 × 力数据时间”与表中位移、照片位移均在约 0.31% 以内，名义照片频率由数量/时间反算也接近表头频率；这是目前较可信的候选同步结果。
- **图 1（暂记为方向 A）**：20 mm/s 一行基本一致；0.2 mm/s 和 2 mm/s 两行的照片位移分别只覆盖速度×力数据时间结果的约 87.4% 和 47.7%，照片数量/频率对应的窗口也明显短于力数据时间。它们更像是照片截取窗口、起止事件或数量统计范围不一致，不能直接当作完整同步记录。
- 两张表的 **DIC 真实采样频率均为空**。因此目前最多能核验“相机文件数量与力数据的粗匹配”，还不能证明 MatchID 输出帧与力传感器逐帧对应。

图 1/图 2 尚未标明 X/Y，本文不擅自猜测轴向。待你确认后，将 A/B 全部替换为 X/Y；计算和判定不受这个命名影响。

## 1. 计算约定

截图中力传感器频率均为 1000 Hz。对每一行同时保留两种时间定义：

\[
T_{N}=N_F/f_F,\qquad T_{\mathrm{interval}}=(N_F-1)/f_F
\]

其中 `N_F` 是导出的力数据行数。前者适合把每一行看成一个采样周期，后者适合首尾样本时间戳分别为 0 和 `(N_F-1)/f_F` 的情况。论文和最终脚本不应再用“力的数量”猜时间，必须以原始 `t_machine` 首尾时间戳为准。

照片数量和表头频率只能给出粗略窗口：

\[
T_{\mathrm{photo,rough}}=N_{\mathrm{img}}/f_{\mathrm{img,header}},\qquad
f_{\mathrm{img,count}}=N_{\mathrm{img}}/T_F
\]

若存在丢帧、首尾裁剪或触发延迟，这两个值都不是有效的逐帧时间。逐帧匹配必须使用照片时间戳或硬件触发编号。

## 2. 按截图数值复核

误差定义：

- `v×T_F`：速度与截图中力数据时间相乘得到的位移（mm）；
- `力位移误差`：`(力位移表 − v×T_F)/(v×T_F)`；
- `照片位移误差`：`(照片位移 − v×T_F)/(v×T_F)`；
- `ΔT_count`：截图力数据时间 − `N_F/1000`（ms）；
- `f_img,count`：照片数量 ÷ 截图力数据时间。

| 暂定方向 | 速度 (mm/s) | 力数量 `N_F` | 照片数量 `N_img` | 表头照片频率 (Hz) | 力数据时间 `T_F` (s) | `N_F/1000` (s) | `ΔT_count` (ms) | `v×T_F` (mm) | 力位移表 (mm) | 力位移误差 | 照片位移 (mm) | 照片位移误差 | `f_img,count` (Hz) | 初步判定 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| A（图 1） | 0.2 | 14735 | 130 | 10 | 14.754 | 14.735 | +19 | 2.9508 | 2.9492 | −0.054% | 2.58 | −12.566% | 8.811 | 力位移一致；照片窗口约 13.0 s，短于力记录，未通过完整窗口核验 |
| A（图 1） | 2.0 | 2789 | 134 | 100 | 2.789 | 2.789 | 0 | 5.5780 | 5.6016 | +0.423% | 2.66 | −52.313% | 48.046 | 力位移可接受；照片窗口约 1.34 s，明显不是 2.789 s 的完整记录 |
| A（图 1） | 20 | 402 | 81 | 200 | 0.401 | 0.402 | −1 | 8.0200 | 8.00 | −0.249% | 8.00 | −0.249% | 201.995 | 位移与频率粗匹配通过，仍需原始时间戳确认 |
| B（图 2） | 0.2 | 179260 | 1825 | 10 | 179.516 | 179.260 | +256 | 35.9032 | 35.9 | −0.009% | 35.9 | −0.009% | 10.166 | 位移一致；力数量与表内时间差 0.256 s，需查时间戳/统计范围 |
| B（图 2） | 2.0 | 15328 | 1558 | 101 | 15.347 | 15.328 | +19 | 30.6940 | 30.6 | −0.306% | 30.6 | −0.306% | 101.518 | 位移和频率粗匹配，仍需查起止事件 |
| B（图 2） | 20 | 1889 | 711 | 375 | 1.890 | 1.889 | +1 | 37.8000 | 37.8 | 0.000% | 37.8 | 0.000% | 376.190 | 位移一致；反算频率约 376.2 Hz，需以帧时间戳为准 |

### 2.1 关键数字怎么解释

- 图 1 的 0.2 mm/s：`0.2×14.754=2.9508 mm`，与力位移 `2.9492 mm` 只差 0.054%；但照片位移为 `2.58 mm`，相差 12.566%。`130/10=13.0 s` 也只覆盖力数据表时间的约 88.1%。
- 图 1 的 2 mm/s：`2×2.789=5.578 mm`，与力位移 `5.6016 mm` 差 0.423%；但照片位移 `2.66 mm` 只相当于约 `1.33 s` 的运动，不能和 2.789 s 的完整力记录直接配对。
- 图 1 的 20 mm/s：`20×0.401=8.02 mm`，两个位移栏均为 8 mm，误差约 0.25%，这是图 1 中唯一同时通过位移窗口检查的一行。
- 图 2 三行的位移均与 `v×T_F` 一致（最大约 0.31%）。不过图 2 的 0.2 mm/s 行仍有 `179260/1000=179.260 s` 与表内 `179.516 s` 的 0.256 s 差异；这说明位移巧合一致不等于计数、时间戳和事件边界已经完全统一。

### 2.2 照片粗窗口复核

下表只检查“照片数量 ÷ 表头频率”能否覆盖截图中的力数据时间，不能替代逐帧时间戳：

| 暂定方向 | 速度 (mm/s) | 照片粗窗口 `N_img/f_header` (s) | 力数据时间 (s) | 照片窗口/力时间 |
|---|---:|---:|---:|---:|
| A（图 1） | 0.2 | 13.000 | 14.754 | 88.1% |
| A（图 1） | 2.0 | 1.340 | 2.789 | 48.0% |
| A（图 1） | 20 | 0.405 | 0.401 | 101.0% |
| B（图 2） | 0.2 | 182.500 | 179.516 | 101.7% |
| B（图 2） | 2.0 | 15.426 | 15.347 | 100.5% |
| B（图 2） | 20 | 1.896 | 1.890 | 100.3% |

图 1 的前两行需要优先查清“是否只导出了部分照片、照片是否有预触发/后触发、力数据是否包含加载前后段”。在这个问题解释清楚之前，不能用补一个比例因子的方法把两段数据硬拼在一起。

## 3. 不能遗漏的同步事实

### 3.1 名义频率相同不等于逐帧同步

力传感器 1000 Hz、相机 10/100/200/101/375 Hz 时，每张照片对应的力采样数约为：

| 表头照片频率 (Hz) | 理想力样本/帧 `1000/f` | 处理方式 |
|---:|---:|---|
| 10 | 100 | 可做整段平均或插值，但仍要保留时间戳 |
| 100 | 10 | 可整除抽取的候选频率；仍不能代替触发号 |
| 200 | 5 | 可整除抽取的候选频率；检查是否有丢帧 |
| 101 | 9.901 | 不能按固定整数抽取，必须按时间戳插值 |
| 375 | 2.667 | 不能按固定整数抽取，必须按时间戳插值；快速变化段需检查带宽与抗混叠 |

因此 VFM 的关键不是让两个频率都写成同一个数字，而是对每一帧建立唯一的 `t_image`，再把 `F_x/F_y`、机器位移和边界条件插值到这个时间：

```text
frame_id, file, t_image, trigger_id
        │
        ├── MatchID 2019 输出：U,V,W、应变、相关系数、质量标记
        └── 力/位移时间序列：在 t_image 处插值 Fx,Fy,ux,uy
```

### 3.2 当前表应改名的字段

截图中的“力数据单位移”和“力数据算时”含义不够明确。下一版 CSV/表格请固定为：

- `force_sample_count`：力数据行数；
- `force_sampling_rate_Hz`：DAQ 设定频率；
- `machine_time_start_s`、`machine_time_end_s`、`machine_duration_s`：由原始时间戳计算；
- `machine_displacement_x_mm`、`machine_displacement_y_mm`：明确轴向和单位；
- `image_frame_count_saved`：实际保存的图像数；
- `camera_frequency_set_Hz`：软件设定值；
- `image_time_start_s`、`image_time_end_s`、`camera_frequency_actual_Hz`：由帧时间戳计算；
- `dic_frame_count_output`、`dic_frequency_actual_Hz`：MatchID 实际输出，而不是相机设定值；
- `sync_offset_s`、`sync_residual_ms`：事件对齐偏移和逐帧残差。

## 4. X/Y 方向的细化对照表

### 4.1 当前三档预试验目标（先用 A/B，待确认 X/Y）

| 方向映射 | 速度 (mm/s) | 力采集频率 (Hz) | 照片频率设定 (Hz) | 当前截图频率 (Hz) | 理想力样本/帧 | 目标用途 | 首要核验 |
|---|---:|---:|---:|---:|---:|---|---|
| A（图 1，X/Y 待确认） | 0.2 | 1000 | 10 | 10 | 100 | 长时间低速基线 | 照片是否覆盖完整力记录；起止触发 |
| A（图 1，X/Y 待确认） | 2 | 1000 | 100 | 100 | 10 | 中速同步基线 | 照片数量为何只覆盖约半段力记录 |
| A（图 1，X/Y 待确认） | 20 | 1000 | 200 | 200 | 5 | 高速预试验 | 丢帧与帧间时间抖动 |
| B（图 2，X/Y 待确认） | 0.2 | 1000 | 10 | 10 | 100 | 长时间低速基线 | 0.256 s 的力计数/时间差 |
| B（图 2，X/Y 待确认） | 2 | 1000 | 101 | 101 | 9.901 | 中速非整除频率 | 必须按时间戳插值 |
| B（图 2，X/Y 待确认） | 20 | 1000 | 375 | 375 | 2.667 | 高速预试验 | 时钟漂移、丢帧、抗混叠 |

如果你确认“图 1=X、图 2=Y”，直接把 A/B 替换为 X/Y；若相反则交换轴名，不需要重算。

### 4.2 每一行试验必须填满的最终记录表

| 字段 | X 方向 | Y 方向 | 来源/计算规则 |
|---|---|---|---|
| `test_id` | 待填 | 待填 | 例：`X_v02_r01` |
| `speed_mm_s` | 待填 | 待填 | 机器指令或实测速度 |
| `t_machine_start/end_s` | 待填 | 待填 | 原始力/位移 CSV 时间戳 |
| `force_sample_count` | 待填 | 待填 | 不再手工从截图抄写 |
| `force_sampling_rate_Hz` | 1000 | 1000 | 设备设置；实际时基仍查时间戳 |
| `Fx/Fy` 或对应轴向力 | 待填 | 待填 | 明确通道、符号、单位 N |
| `ux/uy` 或对应机器位移 | 待填 | 待填 | 明确通道、符号、单位 mm |
| `frame_id_first/last` | 待填 | 待填 | 相机文件清单 |
| `t_image_start/end_s` | 待填 | 待填 | 文件时间戳/硬件触发时间 |
| `image_frame_count_saved` | 待填 | 待填 | 检查缺号、重复号和丢帧 |
| `camera_frequency_actual_Hz` | 待填 | 待填 | `(N_img−1)/(t_last−t_first)`，只作摘要 |
| `dic_frame_count_output` | 待填 | 待填 | MatchID 导出行数/帧数 |
| `dic_frequency_actual_Hz` | 待填 | 待填 | MatchID 输出时间戳计算 |
| `sync_offset_s` | 待填 | 待填 | 触发事件或相关峰确定 |
| `sync_residual_ms` | 待填 | 待填 | 每帧匹配后的残差，不能只写一个名义频率 |
| `dic_quality` | 待填 | 待填 | 相关系数、Sigma、ShapeIndex、缺失点比例 |
| `vfm_eligible` | 待填 | 待填 | 由时间戳、质量和边界覆盖共同判定 |

## 5. 下一次数据处理顺序（固定）

1. **先锁定事件边界**：在机器、力传感器、相机和 MatchID 文件中记录同一个 `test_id`、开始事件、结束事件和触发编号。
2. **建立两个清单**：`machine_samples.csv`（含 `t_machine`）和 `image_manifest.csv`（含 `frame_id`、文件名、`t_image`、丢帧标志）。
3. **计算真实频率但不以它配对**：真实频率用于报告稳定性；逐帧配对使用 `t_image`。
4. **按时间戳插值**：对每个 `t_image` 插值得到同一时刻的 `Fx/Fy`、机器位移和边界条件；不得用“第 N 张照片对应第 N×10 个力样本”的固定整数规则处理 101 Hz 和 375 Hz。
5. **再读取 MatchID 2019 输出**：确认每帧的 `U,V,W`、应变、相关系数和质量字段与 `frame_id` 一一对应；当前 DIC 实际频率为空，不能进入 VFM。
6. **做三项门检**：位移闭合（速度×时间）、帧时间单调且无未解释缺号、同步残差和 DIC 质量均达到预试验约定。门检失败时保留原始文件并标记 `vfm_eligible=false`，不删除数据。
7. **最后才运行 VFM**：使用通过门检的帧—力对，输出参数、残差、独立路径预测和参数对同步误差的敏感性。

## 6. 需要你确认的最小信息

- 图 1 和图 2 分别对应 X、Y 哪个方向；
- “力数据位移”和“力数据时间”两列的确切定义，以及时间列是否来自原始时间戳；
- 每行照片是否从同一个硬件触发开始、是否存在预触发/后触发或手工裁剪；
- 照片数量是否为全部保存帧，还是仅导入 MatchID 的帧；
- MatchID 2019 导出中是否有帧号/时间戳；若只有连续编号，需要提供首帧时间和相机时基；
- 力传感器、机器位移和相机是否共享硬件触发或共同事件信号。

在这些信息补齐前，可以把图 2 的三档作为“候选同步基线”继续做格式和流程测试，但不要把图 1 的前两行或任何无 `t_image` 的结果直接用于 VFM 本构拟合。

## 7. 没有原始时间列时的数量估算（不覆盖 Excel 的 `T` 列）

本节只在没有原始时间列时提供估算：采用你确认可信的速度 `v`、照片数量 `N_img`、力数据数量 `N_F`，并暂按力传感器名义 1000 Hz 计算。**一旦 Excel 中存在时间列，必须以第 8 节的时间戳结果为准，不再用本节的 `N_F/1000` 替换它。**

\[
T_F=N_F/1000,\quad d_F=vT_F,\quad
f_{\mathrm{img,actual}}=N_{\mathrm{img}}/T_F
\]

这样得到的 `f_img,actual` 是“如果照片完整覆盖力记录且没有丢帧时”的反算值；它不是相机硬件时钟的最终证明。照片按原设定频率计算的窗口和位移另列出来，用来发现裁剪或起止事件问题。

| 暂定方向 | 速度 (mm/s) | `N_F` | `N_img` | 相机设定频率 (Hz) | 重算力数据时间 `T_F` (s) | 重算力位移 `d_F` (mm) | 重算照片实际频率 `N_img/T_F` (Hz) | 按设定频率的照片窗口 (s) | 按设定频率的照片位移 (mm) | DIC 实际频率（若每张照片均处理） |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A（图 1） | 0.2 | 14735 | 130 | 10 | 14.735 | 2.947 | 8.823 | 13.000 | 2.600 | 条件值 8.823 Hz |
| A（图 1） | 2 | 2789 | 134 | 100 | 2.789 | 5.578 | 48.046 | 1.340 | 2.680 | 条件值 48.046 Hz |
| A（图 1） | 20 | 402 | 81 | 200 | 0.402 | 8.040 | 201.493 | 0.405 | 8.100 | 条件值 201.493 Hz |
| B（图 2） | 0.2 | 179260 | 1825 | 10 | 179.260 | 35.852 | 10.181 | 182.500 | 36.500 | 条件值 10.181 Hz |
| B（图 2） | 2 | 15328 | 1558 | 101 | 15.328 | 30.656 | 101.644 | 15.426 | 30.851 | 条件值 101.644 Hz |
| B（图 2） | 20 | 1889 | 711 | 375 | 1.889 | 37.780 | 376.390 | 1.896 | 37.920 | 条件值 376.390 Hz |

### 7.1 如何使用这张估算表

- 如果没有原始时间列，才可暂填 `T_F`：图 1 三行为 **14.735 s、2.789 s、0.402 s**；图 2 三行为 **179.260 s、15.328 s、1.889 s**。
- 如果没有原始时间列，才可暂填 `d_F`：图 1 三行为 **2.947 mm、5.578 mm、8.040 mm**；图 2 三行为 **35.852 mm、30.656 mm、37.780 mm**。
- 如果没有原始时间列，才可将 `N_img/T_F` 作为照片实际频率候选：图 1 三行为 **8.823 Hz、48.046 Hz、201.493 Hz**；图 2 三行为 **10.181 Hz、101.644 Hz、376.390 Hz**。
- “按设定频率的照片位移”只能说明照片数量和设定频率构成了多长的照片窗口，不能替代真实位移。例如图 1 的 0.2 mm/s 行为 `0.2×(130/10)=2.600 mm`，这并不等于完整力记录的 `2.947 mm`，说明两者窗口仍未证明相同。
- **DIC 真实频率不能仅凭照片数量确定。** 表中条件值只在“所有保存照片都成功进入 MatchID、没有跳帧、首尾时间一致”时成立。正式填写必须按统一的样本定义计算（首尾间隔定义为 `(N_DIC−1)/(t_DIC_last−t_DIC_first)`，采样点数定义为 `N_DIC/T_DIC`），并保留 MatchID 导出的帧号/时间戳。

### 7.2 采样点数定义的替代结果

若实验室把首尾样本之间的间隔定义为实际时长，应使用：

\[
T_{\mathrm{interval}}=(N_F-1)/1000,
\qquad d_{\mathrm{interval}}=vT_{\mathrm{interval}}
\]

与上表相比，每行时间减少 `0.001 s`，位移减少 `v×0.001 mm`；例如图 1 的三行变为 **14.734 s/2.9468 mm、2.788 s/5.576 mm、0.401 s/8.020 mm**。两种定义都可以用于内部记录，但同一试验系列必须固定一种，并在方法部分写明；有原始时间戳时，以 `t_last−t_first` 为唯一最终值。

## 8. Excel 原始数据核验：力传感器名义 1000 Hz，时间戳优先

### 8.1 直接证据

`D:\C盘迁移\Desktop\yuan\data\XY\20250529\20250529` 下的这些 `.xls` 文件虽然扩展名为 `.xls`，实际可以按 Excel 工作簿读取。每个工作簿都有 `Pos`、`Speed`、`Press` 三个工作表；`Speed` 和 `Press` 的第一列均为 `T`，正常相邻时间差为 **0.001 s**，因此设备的**名义采样频率为 1000 Hz**。

部分文件出现 `0.002 s` 的时间间隔。这表示时间轴中有间隔缺口或导出时跳过了一个采样时刻；它不改变设备的名义设定值，但会使按整段首尾时间计算的有效平均频率略低于 1000 Hz。因此后续同步必须读取 `T`，不能用 `力数据数量/1000` 代替 `T` 的首尾差。

本目录中其余 `xy-...` Excel 工作簿也呈现相同模式：中位时间步长为 `0.001 s`，只有少量 `0.002 s` 间隔。因此 1000 Hz 是这批数据共同的名义力采样频率，而不是某一个单独文件的偶然值。

### 8.2 与两张截图逐行对应的 Excel 证据

下面用 `Speed` 工作表行数与截图“力的数量”对应；`Press` 行数相同。`T_end` 是该表最后一个有效时间值，正好复现截图中的“力数据时间”。有效平均频率采用首尾时间定义：

\[
f_{\mathrm{span}}=(N_F-1)/(T_{\mathrm{end}}-T_{\mathrm{start}})
\]

| 截图方向 | 对应 Excel 文件 | `N_F` | `T_start` (s) | `T_end` (s) | 中位 `ΔT` (s) | `0.002 s` 间隔数 | `f_span` (Hz) | 结论 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 图 1，A（文件名 X） | `x-05-0.1_state_20250529133138_20250529133153.xls` | 14735 | 0 | 14.754 | 0.001 | 20 | 998.644 | 名义 1000 Hz；截图时间 14.754 s 与 Excel 一致 |
| 图 1，A（文件名 X） | `x-6-1_state_20250529133928_20250529133930.xls` | 2789 | 0 | 2.789 | 0.001 | 1 | 999.641 | 名义 1000 Hz；截图时间一致 |
| 图 1，A（文件名 X） | `x-7-10_state_20250529134610_20250529134611.xls` | 402 | 0 | 0.401 | 0.001 | 0 | 1000.000 | 名义与有效频率均为 1000 Hz |
| 图 2，B（文件名 Y） | `y-9-0.1_state_20250529140828_20250529141127.xls` | 179260 | 0 | 179.516 | 0.001 | 257 | 998.568 | 名义 1000 Hz；截图时间 179.516 s 与 Excel 一致 |
| 图 2，B（文件名 Y） | `y-10-1_state_20250529141618_20250529141633.xls` | 15328 | 0 | 15.347 | 0.001 | 20 | 998.697 | 名义 1000 Hz；截图时间一致 |
| 图 2，B（文件名 Y） | `y-11-10_state_20250529142519_20250529142520.xls` | 1889 | 0 | 1.890 | 0.001 | 2 | 998.942 | 名义 1000 Hz；截图时间一致 |

文件名中的 `x-`、`y-` 与每行数量/时间完全对应，因此目前最有证据的轴向映射是：**图 1 = X 方向，图 2 = Y 方向**。如果你的试验记录另有轴向定义，以试验记录为最终依据。

### 8.3 最终应如何填写力传感器频率

- **设备/软件设置栏**：填写 **1000 Hz**。这是由 Excel `T` 列的 0.001 s 名义时间步长直接支持的采样频率。
- **数据质量或同步分析栏**：同时记录 `f_span` 和 `0.002 s` 间隔数。当前六个对应文件的 `f_span` 约为 **998.568–1000.000 Hz**；它描述的是本段文件的有效时间轴，不应回填到设备设置栏冒充硬件频率。
- **试验持续时间**：使用 `T_end−T_start`，即截图中的 **14.754、2.789、0.401、179.516、15.347、1.890 s**；这些值已被 Excel 的 `T` 列验证。
- **力数据位移**：使用 `速度×(T_end−T_start)`。在截图的六个速度/时间组合下，分别为 **2.9508、5.5780、8.0200、35.9032、30.6940、37.8000 mm**；这是基于 Excel 时间轴的结果。

因此，本次 Excel 核验后的最终判断是：**力传感器名义频率确实为 1000 Hz；截图中的力数据时间不是简单按数量除以 1000 得到的，而是来自包含少量 0.002 s 时间间隔的 Excel 时间列。后续同步以 Excel `T` 时间戳为准。**

## 9. XZ 数据表：先对齐相机/DIC，力通道待补

### 9.1 XZ 目录中实际存在的证据

`D:\C盘迁移\Desktop\yuan\data\XZ\xz_tu数据` 中没有与 XY 同结构的 `Pos/Speed/Press` Excel 文件。XZ 目录目前包含：

- JPEG 图像和对应的 `.jpg.dat` 压缩数据；
- 3 个 `CIHX` 相机元数据文件；
- 3 个 MatchID 2019 `Job.m2inp` 文件；
- 3 个以分号分隔的 CSV 文件，表头为 `Serie;X;Y`。

因此，XZ 的相机记录频率可以从 `CIHX` 的 `recordRate` 读取；XZ 的力传感器频率和力时间目前不能从该目录直接确定。若这些 XZ 试验使用与 XY 相同的设备采集程序，可以把 1000 Hz 作为“沿用的设备设置”记录，但必须标注为**来自 XY 的条件假设**，不能写成 XZ 文件已经证明的事实。

### 9.2 XZ 文件、帧数和相机频率对照表

`CIHX` 中的 `totalFrame`、文件夹内 JPEG 数量和 `Job.m2inp` 中的 `<Deformed$image>` 行数在前三个序列中一致。`recordRate` 是相机记录频率；在没有逐帧时间戳时，下面的持续时间只是按首尾帧估算：

\[
T_{\mathrm{camera,est}}=(N_{\mathrm{img}}-1)/f_{\mathrm{camera}}
\]

| XZ 序列 | JPEG 数量 | CIHX `recordRate` (Hz) | CIHX `totalFrame` | CIHX `recordedFrame` | `Job.m2inp` 帧数 | DIC CSV 行数/唯一帧号 | DIC 缺帧 | 相机持续时间估算 (s) | 当前状态 |
|---|---:|---:|---:|---:|---:|---:|---|---:|---|
| `Z-0.1-06_C001H001S0001` | 227 | 60 | 227 | 2269 | 227 | 454 / 227 | 无 | 3.7667 | 图像、CIHX、MatchID 输入和 CSV 帧号对齐 |
| `（Z）-1-07_C001H001S0001` | 546 | 500 | 546 | 5457 | 546 | 1092 / 546 | 无 | 1.0900 | 图像、CIHX、MatchID 输入和 CSV 帧号对齐 |
| `（Z）-1-08_C001H001S0001` | 178 | 500 | 178 | 2173 | 178 | 334 / 167 | 131–141，共 11 帧 | 0.3540 | 图像完整，但 DIC CSV 缺 11 个帧号，不能当作完整 DIC 序列 |
| `XZ-10-04_C001H001S0001` | 81 | **缺 CIHX** | — | — | — | 无 CSV | 未知 | 未知 | 只有连续 JPEG 374–454，缺相机频率和 MatchID 输入 |

XZ 的 CSV 不是时间序列：每个唯一 `X` 帧号恰好有两行记录，`Y` 是对应的数值。因此：

- `Z-0.1-06.csv` 的 454 行 = 227 个唯一帧号 × 2；
- `（Z）-1-07.csv` 的 1092 行 = 546 个唯一帧号 × 2；
- `（Z）-1-08.csv` 的 334 行 = 167 个唯一帧号 × 2，但 JPEG/MatchID 输入有 178 帧，帧号 131–141 没有导出到 CSV。

不能把 XZ CSV 的 `X` 列当作秒，也不能由 `X` 的整数步长推断力传感器频率。

### 9.3 可直接使用的 XZ 对照表

| `test_id` | 图像帧数 | 相机名义频率 | 相机时间窗口 | 力数据数量 | 力采样频率 | 力时间窗口 | 力位移 | DIC 输出帧数 | 是否可进入同步/VFM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `XZ_Z-0.1-06` | 227 | 60 Hz | 3.7667 s（估算） | 待补 | 待补；若沿用 XY 则暂记 1000 Hz | 待补 | `速度×力时间` | 227（CSV 有效） | 可做相机/DIC流程基线；力通道补齐后再判定 |
| `XZ_Z-1-07` | 546 | 500 Hz | 1.0900 s（估算） | 待补 | 待补；若沿用 XY 则暂记 1000 Hz | 待补 | `速度×力时间` | 546（CSV 有效） | 可做相机/DIC流程基线；力通道补齐后再判定 |
| `XZ_Z-1-08` | 178 | 500 Hz | 0.3540 s（估算） | 待补 | 待补；若沿用 XY 则暂记 1000 Hz | 待补 | `速度×力时间` | 167（缺 11 帧） | 暂不进入 VFM，先补齐或剔除缺帧 |
| `XZ_XZ-10-04` | 81 | 未知 | 未知 | 待补 | 未知 | 待补 | `速度×力时间` | 未导出 | 暂不进入 VFM，先补 CIHX/相机设置和力文件 |

### 9.4 XY 与 XZ 的“对上”规则

#### XY

XY 已经具备机器 Excel 时间轴：

```text
Excel Speed/Press 的 T
        ↓
力数据时间 = T_last − T_first
        ↓
以 t_image 插值得到 Fx、Fy、机器位移
        ↓
匹配 MatchID 2019 的 DIC 帧
```

XY 的设备力采样频率填 **1000 Hz**，时间和位移则使用 Excel `T` 列。

#### XZ

XZ 当前只有相机/图像侧证据：

```text
CIHX recordRate
        ↓
图像帧时间候选
        ↓
MatchID 输入帧号与 DIC CSV 帧号核对
        ↓
等待对应的力/位移 Excel 或 CSV
        ↓
按共同 test_id 和时间戳插值匹配
```

XZ 在取得力文件前，只能完成“相机帧—MatchID 帧”对齐，不能完成“照片—力传感器—VFM”对齐。

### 9.5 这次对齐后的执行结论

- XY：力传感器名义频率已经由 Excel `T` 列确认为 **1000 Hz**；截图中的力数据时间可直接保留，力位移改用速度乘 Excel 实际时间。
- XZ：`Z-0.1-06` 为 60 Hz、`（Z）-1-07` 和 `（Z）-1-08` 为 500 Hz；前两组图像/MatchID 帧数一致，第三组 DIC CSV 缺 11 帧；`XZ-10-04` 缺相机元数据。
- XZ 目录没有力传感器 Excel，因此不能从 XZ 文件本身证明力采样频率。只有找到对应的机器 `Pos/Speed/Press` 文件或包含 `t_machine` 的力 CSV 后，才能把 XZ 表格的“力数量、力时间、力位移、同步残差”填实。

## 10. 首帧/末帧事件边界：加载起点与破坏终点

### 10.1 先固定术语

你说的“最后一张是发生锻炼的阶段”，本页暂按**发生断裂、明显破坏或目标大变形**理解；“锻炼”若实际想表达的是**屈服**，只需把 `last_event_type` 改为 `yield`，边界记录方法不变。由于首帧和末帧会直接影响 DIC 位移基准、力插值和 VFM 本构识别，不能只按文件名的第一张/最后一张来决定。

本项目固定区分三个帧：

1. `frame_reference`：零载或预载稳定后的参考帧，只用于 DIC 位移/应变参考，**不等同于刚受到力的首帧**。
2. `frame_first_valid`：加载开始后第一张仍完整覆盖试样、且力信号已离开基线的有效分析帧；这是 VFM 时间窗口的起点。
3. `frame_last_valid`：试验结束事件之后最后一张仍能确认试样状态的有效分析帧；若目标是断裂，必须能同时给出破坏图像或力骤降证据。

因此，原始目录中的“第一张”和“最后一张”只作为候选，不自动获得有效资格。首尾之外的预触发、后触发和超出 ROI 的图片保留在原目录，并在清单中标记为 `out_of_window`，不删除。

### 10.2 硬性边界规则

#### 首帧：刚受到力的阶段

- 先保留至少一张加载前的 `frame_reference`，用来计算 DIC 的零位移/零应变。
- 沿机器 `T` 时间轴寻找加载起点：力通道或机器位移首次稳定离开基线，并且相邻图像仍完整覆盖试样。
- `frame_first_valid` 取加载起点之后的第一张完整图像；若加载起点落在两张图之间，取其后的第一张，并记录 `onset_bracket`（前一帧、后一帧）。
- 不能用“第一张照片力值恰好为 0”作为规则；传感器零漂、夹具预紧和触发延迟会使该值不可靠。

#### 末帧：断裂/破坏或大变形阶段

- 若能看到断裂、脱落或明显破坏，`last_event_type=fracture/damage`，取事件发生后最后一张仍看清试样状态的图像。
- 若没有可见断裂，但力出现不可恢复的骤降，`last_event_type=force_drop`，同时保留力峰值、下降幅度和对应帧号。
- 若试验因位移上限或人工停止结束，`last_event_type=displacement_limit/test_stop`；不能把它写成 `fracture`。
- 若最后一张已经没有试样或 ROI 离开视场，不能作为 `frame_last_valid`；应向前回溯到最后一张完整覆盖试样的图像，并把后续图片标为 `out_of_window`。
- 末帧必须同时具备**图像证据**（断裂、破坏、完整试样或明确结束状态）和**机器/力信号证据**（峰后骤降、位移限位或停止事件）各至少一项；若缺任一侧证据，状态为 `endpoint_review`，不得直接进入 VFM。

### 10.3 现有序列的首末帧检查

| `test_id`/文件夹 | 图像数 | 首帧观察 | 末帧观察 | 当前边界判定 | 下一步 |
|---|---:|---|---|---|---|
| `X-05-0.1-01` | 165（截图写 130） | `Img000000.jpg` 可见完整试样和散斑 | `Img000164.jpg` 试样基本离开视场 | 末帧不合格；不能直接称为破坏终点 | 建立清单，向前找最后完整 ROI，并核对为何截图只有 130 张 |
| `X-06-1.0-01` | 133（截图写 134） | 待按事件标注 | 待按事件标注 | 数量不一致，暂不判定 | 核对缺帧/重复帧及触发范围 |
| `X-07-10-01` | 81 | 待按事件标注 | 待按事件标注 | 文件数与截图一致，但未完成事件核验 | 用 Excel `T` 与图像帧号建立起止清单 |
| `Y-09-0.1-02` | 1825 | `Img000000.jpg` 试样完整、未破坏 | `Img001824.jpg` 中部已出现明显断裂 | 可作为断裂终点候选；仍需绑定力骤降时刻 | 在 `Press.T` 中定位峰后下降并记录 `frame_last_valid` |
| `Y-10-1-01` | 1558 | 待按事件标注 | 末端力通道出现约 −1243/−1245 的大幅值 | 末端存在强信号事件，但事件类型未定 | 检查末端图像是否断裂/夹具限位，不能仅凭力值命名 |
| `Y-11-10-01` | 711 | 首行力通道存在较大负值 | 末行接近基线 | 首末语义可能反向或含预载/卸载段 | 先按事件切出加载段，再确定末帧 |
| `XZ_Z-0.1-06` | 227 | 图像/MatchID 帧号完整 | 力通道缺失，末端事件未知 | 只能确认相机—DIC 帧连续 | 补对应力/位移文件及事件记录 |
| `XZ_Z-1-07` | 546 | 首帧完整 | 力通道缺失，末端事件未知 | 只能确认相机—DIC 帧连续 | 补对应力/位移文件；再做首末事件判定 |
| `XZ_Z-1-08` | 178 | 图像帧完整 | DIC CSV 缺 131–141 共 11 帧 | 序列不完整，末帧暂不合格 | 补导出或剔除缺帧区间后再定边界 |
| `XZ_XZ-10-04` | 81 | 只有 JPEG | 相机元数据、DIC 输入和力文件均缺失 | 无法判定 | 先补 CIHX/Job/力数据 |

其中 `Y-09-0.1-02` 是目前唯一已经有图像侧断裂证据的序列；`X-05-0.1-01` 的最后一张则明确说明了“文件末尾 ≠ 试样末帧”。这两条证据足以把“首尾事件判定”设为后续所有 XY/XZ 处理的硬门槛。

### 10.4 首末帧清单（每个试验一行）

正式处理时，在 `image_manifest.csv` 或同等表格中至少保留以下字段。字段值没有证据时填 `待核验`，不填猜测值：

| 字段 | 含义 |
|---|---|
| `test_id` | 与机器、力传感器、相机和 MatchID 工程一致的试验编号 |
| `frame_id_first_file` / `frame_id_last_file` | 文件夹按编号排序后的首、末文件帧号 |
| `frame_reference` | 零载/预载稳定参考帧 |
| `frame_first_valid` | 加载起点后的第一张有效分析帧 |
| `first_event_type` | `load_onset`、`preload` 或 `endpoint_review` |
| `t_first_image_s` | 首个有效分析帧的图像时间；无时间戳时不得伪造 |
| `frame_last_valid` | 破坏/结束后最后一张仍覆盖试样的有效帧 |
| `last_event_type` | `fracture`、`damage`、`force_drop`、`yield`、`displacement_limit` 或 `test_stop` |
| `t_last_image_s` | 末个有效帧的图像时间 |
| `force_at_first` / `force_at_last` | 与图像帧插值到同一时刻的力值及通道 |
| `position_at_first` / `position_at_last` | 与图像帧对应的机器位移 |
| `trigger_id` | 共同硬件触发或事件编号；没有则写 `未提供` |
| `visual_evidence` | 试样完整、断裂、脱落、离开 ROI 等可复核描述 |
| `signal_evidence` | 基线离开、峰值、骤降、位移限位等可复核描述 |
| `boundary_rule` | 本节 10.2 的具体规则编号/文字 |
| `endpoint_review` | `pass`、`review` 或 `fail` |
| `vfm_eligible` | 只有首末边界、时间映射、DIC 质量均通过才填 `true` |

### 10.5 固定执行顺序

1. 按帧号建立 `image_manifest`，检查缺号、重复号、文件首尾和 ROI 覆盖；不按截图照片数量直接截取。
2. 从 Excel `T` 列读取机器/力时间，先确定加载前基线、加载起点、峰值和结束事件。
3. 将相机帧时间或共同触发号映射到机器时间；没有时间戳时，只能写“估算”，不能宣称逐帧同步。
4. 分别填写 `frame_reference`、`frame_first_valid` 和 `frame_last_valid`，并给出图像与信号证据。
5. 只对 `frame_first_valid..frame_last_valid` 运行 MatchID 2019 3D DIC 和后续 VFM；窗口外图片保留但不参与拟合。
6. 若末帧事件类型是 `test_stop`、`displacement_limit` 或 `endpoint_review`，论文中如实报告，禁止改写为断裂。

首帧/末帧边界没有完成前，表格只能叫“频率/窗口初步核验”，不能称为“照片—力—DIC 已同步”，也不能据此宣称 VFM 本构参数已经可靠。

## 11. 全量盘点修订（2026-09-12）

本页第 9 节只针对已解压的 `XZ/xz_tu数据` 子目录；对 `D:\C盘迁移\Desktop\yuan\data` 的完整范围盘点已单独写入[全量照片—力—DIC 配对清单](07_全量照片-力-DIC配对清单.md)。补充结论如下：

- `XZ.zip` 中心目录实际列出 7 个 XZ 图像序列和 12 个 XZ 力 Excel 文件；因此“XZ 目录没有力 Excel”只能理解为“已解压子目录没有”，不能作为整个 `data` 的结论。
- 当前缺少 `XZ.z01`。XZ 力 Excel 的目录项可以看到，但其工作表内容不能读取；在补齐 `XZ.z01` 或直接提供解压的 XZ `.xls` 前，XZ 的力数量、力时间和力频率仍是待补证据。
- `XZ-10-04` 的完整压缩包目录记录为 454 帧、CIHX `recordRate=1000 Hz`、`Job.m2inp` 454 帧；已解压目录中看到的 374–454 只是 81 帧子集，不能再写成该序列的完整 JPEG 数量。
- 后续 XZ 处理以第 7 页的全量清单为入口；本页保留用于截图数值、XY Excel 时间轴和首末事件规则的历史核验。
